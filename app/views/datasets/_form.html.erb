<%= form_for([@user, @dataset]) do |f| %>

  <% if @dataset.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@dataset.errors.count, "error") %> prohibited
        this dataset from being saved:
      </h2>
      <br />
      <ul>
        <% @dataset.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <br/>
  <p >
    <%= f.label :name, :class=>"form_label"%><br>
    <%= f.text_field :name,  :class=>"form_box"%>
  </p>
  <%= f.submit "Submit", :class=>"submit_buttom" %>
<% end %>